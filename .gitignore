# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Rust build output — the two .wasm artefacts it produces are committed under
# public/, so a fresh clone runs with no Rust toolchain installed.
rust/target

# Measurement output (tools/fidelity.mjs, tools/bench.mjs)
fidelity-results.json
bench-results.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
